name: KDL Server MLFlow Quality

on:
  push:
    branches-ignore:
      - "v*"
    tags-ignore:
      - v*
    paths:
      - "mlflow/**/*"
      - ".github/workflows/kdl-server-mlflow-quality*"

jobs:
  lint-dockerfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: hadolint/hadolint-action@v1.6.0
        with:
          dockerfile: ./mlflow/Dockerfile
          format: tty
          config: ./.hadolint.yml