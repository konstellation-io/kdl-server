name: KDL Server VSCode Dockerfile Quality

on:
  push:
    branches-ignore:
      - "v*"
    tags-ignore:
      - v*
    paths:
      - "vscode/Dockerfile"
      - ".github/workflows/kdl-server-vscode-dockerfile-quality*"

jobs:
  lint-dockerfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: hadolint/hadolint-action@v1.6.0
        with:
          dockerfile: ./vscode/Dockerfile
          format: tty
          config: ./.hadolint.yml
