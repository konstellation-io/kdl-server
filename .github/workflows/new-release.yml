name: New Release Branch

on:
  workflow_dispatch:


jobs:
  new-release:
    uses: konstellation-io/github-workflows/.github/workflows/new-release.yaml@main
    with:
      chart_name: kdl-server
    secrets:
      github_token: ${{ secrets.GITHUB_TOKEN }}

