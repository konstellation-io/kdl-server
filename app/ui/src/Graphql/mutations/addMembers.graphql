mutation AddMembers($input: AddMembersInput!) {
  addMembers(input: $input) {
    id
    members {
      user {
        id
        email
        lastActivity
      }
      accessLevel
      addedDate
    }
  }
}
