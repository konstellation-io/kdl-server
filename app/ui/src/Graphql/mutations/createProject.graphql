mutation CreateProject($input: CreateProjectInput!) {
  createProject(input: $input) {
    id
    name
    description
    favorite
    creationDate
    lastActivationDate
    repository {
      id
      type
      url
      connected
    }
    state
    error
  }
}
