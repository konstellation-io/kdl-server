mutation UpdateMember($input: UpdateMemberInput!) {
  updateMember(input: $input) {
    id
    members {
      user {
        id
        email
        lastActivity
      }
      accessLevel
      addedDate
    }
  }
}
