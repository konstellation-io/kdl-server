query GetKnowledgeGraph($projectId: ID!) {
  knowledgeGraph(projectId: $projectId) {
    items {
      id
      category
      title
      abstract
      authors
      score
      date
      url
      starred
      topics {
        name
        relevance
      }
    }
    topics {
      name
      relevance
    }
  }
}
