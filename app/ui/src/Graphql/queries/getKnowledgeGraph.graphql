query GetKnowledgeGraph($description: String!) {
  knowledgeGraph(description: $description) {
    items {
      id
      category
      title
      abstract
      authors
      score
      date
      url
      topics {
        name
        relevance
      }
    }
    topics {
      name
      relevance
    }
  }
}
