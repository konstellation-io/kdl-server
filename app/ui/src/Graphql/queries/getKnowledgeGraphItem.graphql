query GetKnowledgeGraphItem($id: ID!) {
  knowledgeGraphItem(id: $id) {
    id
    category
    title
    abstract
    authors
    score
    date
    url
    topics {
      name
      relevance
    }
  }
}
